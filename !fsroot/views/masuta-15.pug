html(lang='ja')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width,initial-scale=1.0')
    link(rel="stylesheet" href="css/styles.css")
    link(rel='icon', type='image/ png', size='16x16', href='image/vcicon.png')
    script(src="https://yubinbango.github.io/yubinbango/yubinbango.js" charset="UTF-8")
    title #{title}
  body
    header 
      li.TOP(id='top') ＴＯＰ
      li.TOPs ＞＞
      li.TOP(id='top' ) #{title}
    #app
      .A
        br
        h2 プログラム一覧
          br
        nav.menu(style='border-top: solid #1e366a 1px;border-bottom: solid #1e366a 1px;margin-left:39px')
            ul.list
              .D
                li.masuta(v-for="list in lists" v-on:mouseover="loadlist(list.method)" v-bind:id="list.id") {{list.value}}
              .F(v-show="is__select1")
                li.masuta2(v-for="masutalist in masuta" v-bind:id="masutalist.id") {{masutalist.value}}
              .F(v-show="is__select2")
                li.masuta2(v-for="Salelist in Sales" v-bind:id="Salelist.id") {{Salelist.value}}
              .F(v-show="is__select3")
                li.masuta2(v-for="Purchaselist in Purchase" v-bind:id="Purchaselist.id") {{Purchaselist.value}}
              .F(v-show="is__select4")
                li.masuta2(v-for="stocklist in Stock" v-bind:id="stocklist.id") {{stocklist.value}}
              .F(v-show="is__select5")
                li.masuta2(v-for="productionlist in Production" v-bind:id="productionlist.id") {{productionlist.value}}
              .F(v-show="is__select6")
                li.masuta2(v-for="transferlist in Transfer" v-bind:id="transferlist.id") {{transferlist.value}}
      br
      form.program(class='h-adr' name='program' action='/tyokusou1' onsubmit="return false;" method='POST' )
        .B(style="width:700px")
          li.button(@click='kubun1' id='ku1') 入力/修正
          li.button3(@click='kubun3' id='ku3') 削除
          li.button(id='itiran' style='margin-right:50px') 一覧
          li.button(id='sub') 送信
          span.p-country-name(style="display:none;") Japan
          p.ku(style='width:250px;') 現在の処理 : {{ kubun }}
          hr(style="margin-bottom:10px;margin-top:10px")
          label.koumoku
            | 直送先コード　：
            input.nyu(id='tyokusoucode' name='koumoku1' size="10" type='text', value=`${koumoku[0]}` placeholder='コードを入力' autocomplete="off") 
          br
          br
          label.koumoku
            | 直送先名１　　：
            input.nyu(id='tyokusouname' name='koumoku2' size='60' type='text', value=`${koumoku[1]}`, v-bind:readonly='is_readonly' autocomplete="off")
          br
          br
          label.koumoku
            | 直送先名２　　：
            input.nyu(id='koumoku3' name='koumoku3' size='60' type='text', value=`${koumoku[2]}` v-bind:readonly='is_readonly' autocomplete="off")
          br
          br
          label.koumoku
            | フリガナ　　　：
            input.nyu(id='koumoku9' name='koumoku9' size='60' type='text', value=`${koumoku[3]}` v-bind:readonly='is_readonly' autocomplete="off")
          br
          br
          label.koumoku
            | 郵便番号　　　：
            input.nyu(class='p-postal-code' id='koumoku4' name='koumoku4' type='text', value=`${koumoku[4]}` v-bind:readonly='is_readonly' size="10" maxlength="8" autocomplete="off" placeholder='XXX-XXXX')
          br
          br
          label.koumoku
            | 住所１　　　　：
            input.nyu(class='p-region p-locality p-street-address p-extended-address' id='koumoku5' name='koumoku5' type='text', value=`${koumoku[5]}` v-bind:readonly='is_readonly' size="60" autocomplete="off")
          br
          br
          label.koumoku
            | 住所２　　　　：
            input.nyu(size='60' id='koumoku6' name='koumoku6' type='text', value=`${koumoku[6]}` v-bind:readonly='is_readonly' autocomplete="off")
          br
          br
          label.koumoku
            | ＴＥＬ番号　　：
            input.nyu(id='koumoku7' name='koumoku7' type='text', value=`${koumoku[7]}` size="15" v-bind:readonly='is_readonly' autocomplete="off" placeholder='XXXX-XX-XXXX')
          br
          br
          label.koumoku
            | ＦＡＸ番号　　：
            input.nyu(id='koumoku8' name='koumoku8' type='text', value=`${koumoku[8]}` size="15" v-bind:readonly='is_readonly' autocomplete="off" placeholder='XXXX-XX-XXXX')
          br
          br
          label.koumoku
            | メモ　　　　　：
            input.nyu(id='koumoku10' name='koumoku10' size='60' type='text', value=`${koumoku[9]}` v-bind:readonly='is_readonly' autocomplete="off")
          br
          br
          hr
    script(src='https://cdn.jsdelivr.net/npm/vue/dist/vue.js')
    script(src='script/program.js')
    script(src='script/Directdelivery.js')
    script(src='script/information.js')
    