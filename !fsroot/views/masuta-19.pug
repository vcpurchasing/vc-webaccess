html(lang='ja')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width,initial-scale=1.0')
    link(rel="stylesheet" href="css/styles.css")
    link(rel='icon', type='image/ png', size='16x16', href='image/vcicon.png')
    title #{title}
  body
    header 
      li.TOP(id='top') ＴＯＰ
      li.TOPs ＞＞
      li.TOP(id='top' ) #{title}
    #app
      .A
        br
        h2 プログラム一覧
          br
        nav.menu(style='border-top: solid #1e366a 1px;border-bottom: solid #1e366a 1px;margin-left:39px')
            ul.list
              .D
                li.masuta(v-for="list in lists" v-on:mouseover="loadlist(list.method)" v-bind:id="list.id") {{list.value}}
              .F(v-show="is__select1")
                li.masuta2(v-for="masutalist in masuta" v-bind:id="masutalist.id") {{masutalist.value}}
              .F(v-show="is__select2")
                li.masuta2(v-for="Salelist in Sales" v-bind:id="Salelist.id") {{Salelist.value}}
              .F(v-show="is__select3")
                li.masuta2(v-for="Purchaselist in Purchase" v-bind:id="Purchaselist.id") {{Purchaselist.value}}
              .F(v-show="is__select4")
                li.masuta2(v-for="stocklist in Stock" v-bind:id="stocklist.id") {{stocklist.value}}
              .F(v-show="is__select5")
                li.masuta2(v-for="productionlist in Production" v-bind:id="productionlist.id") {{productionlist.value}}
              .F(v-show="is__select6")
                li.masuta2(v-for="transferlist in Transfer" v-bind:id="transferlist.id") {{transferlist.value}}
      br
      form.program(name='program' action='/purchasingcost1' onsubmit="return false;" method='POST' )
        .B(style="width:700px")
          li.button(@click='kubun1' id='ku1') 入力/修正
          li.button3(@click='kubun3' id='ku3') 削除
          li.button(id='itiran' style='margin-right:50px') 一覧
          li.button(id='sub') 送信
          span.p-country-name(style="display:none;") Japan
          p.ku(style='width:250px;') 現在の処理 : {{ kubun }}
          hr(style="margin-bottom:10px;margin-top:10px")
          label.koumoku
            | 商品コード　　　　：
            input.nyu(id='productcode' name='koumoku[]' size="10" type='text', value=`${koumoku[0]}` placeholder='コードを入力' autocomplete="off") 
            input.nyu(id='productname' name='koumoku[]' size="30" type='text', value=`${koumoku[1]}` style='margin-left:10px' autocomplete="off" tabindex='-1') 
          br
          br
          label.koumoku
            | 仕入先コード　　　：
            input.nyu(id='purchasecode' name='koumoku[]' size="10" type='text', value=`${koumoku[2]}` placeholder='コードを入力' autocomplete="off") 
            input.nyu(id='purchasename' name='koumoku[]' size="30" type='text', value=`${koumoku[3]}` style='margin-left:10px' autocomplete="off" tabindex='-1') 
          br
          br
          label.koumoku
            | 外国仕入先コード　：
            input.nyu(id='purchasefcode' name='koumoku[]' size="10" type='text', value=`${koumoku[4]}` placeholder='コードを入力' autocomplete="off") 
            input.nyu(id='purchasefname' name='koumoku[]' size="30" type='text', value=`${koumoku[5]}` style='margin-left:10px' autocomplete="off" tabindex='-1') 
          br
          br
          label.koumoku
            | 物流費率　　　　　：
            input.nyu(id='koumoku9' name='koumoku[]' size='10' type='text', value=`${koumoku[6]}` v-bind:readonly='is_readonly' autocomplete="off")
          br
          br
          label.koumoku
            | 外国原価　　　　　：
            input.nyu(id='koumoku4' name='koumoku[]' type='text', value=`${koumoku[7]}` v-bind:readonly='is_readonly' size="10" autocomplete="off")
            span(style='font-size:13px;') ドル
          br
          br
          label.koumoku
            | 営業原価　　　　　：
            input.nyu(id='koumoku5' name='koumoku[]' type='text', value=`${koumoku[8]}` v-bind:readonly='is_readonly' size="10" autocomplete="off")
            span(style='font-size:13px;')  円
          br
          br
          label.koumoku
            | 構成商品　　　　　：
            select.selectbox(id='constitution' name='koumoku[]' v-bind:readonly='is_readonly' style='width:103px;height:31px' onchange='selectboxChange(this.selectedIndex);')
              option 　　　　　
              option 構成
              option 非構成
              option 1次構成
          br
          br
          label.koumoku
            | 生産地　　　　　　：
            select.selectbox(id='producingarea' name='koumoku[]' v-bind:readonly='is_readonly' style='width:103px;height:31px' onchange='selectboxChange(this.selectedIndex);')
              option 　　         
              option 国内
              option 海外
          br
          br
          label.koumoku
            | 評価率　　　　　　：
            input.nyu(id='koumoku8' name='koumoku[]' type='text' value=`${koumoku[11]}` size="10" v-bind:readonly='is_readonly' autocomplete="off")
          br
          br
          label.koumoku
            | 備考　　　　　　　：
            input.nyu(id='koumoku10' name='koumoku[]' size='60' type='text', value=`${koumoku[12]}` v-bind:readonly='is_readonly' autocomplete="off")
          br
          br
          hr
          input(id='jun' value=`${jun}` hidden)
          input(id='constitutions' value=`${koumoku[9]}` hidden)
          input(id='producingareas' value=`${koumoku[10]}` hidden)
    script(src='https://cdn.jsdelivr.net/npm/vue/dist/vue.js')
    script(src='script/program.js')
    script(src='script/purchingcost.js')
    script(src='script/information.js')
    